import 'package:flutter/material.dart';

class WealPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '福利',
        style: TextStyle(fontSize: 32.0),
      ),
    );
  }
}
